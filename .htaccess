<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /blims/

    # If file or directory does not exist, send request to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
